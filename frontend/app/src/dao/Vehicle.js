class Vehicle {
    constructor(brand, model, licensePlate, color, year) {
        this.brand = brand;
        this.model = model;
        this.licensePlate = licensePlate;
        this.color = color;
        this.year = year;
    }
}

export default Vehicle;
